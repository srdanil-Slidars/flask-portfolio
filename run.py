"""
Точка входа для запуска Flask-приложения.

Запуск:
    python run.py

Назначение:
- импортирует фабрику приложения;
- создает экземпляр Flask;
- запускает локальный сервер разработки.
"""

from app import create_app

# Создаем экземпляр приложения через фабричный метод
app = create_app()

if __name__ == "__main__":
    # debug=True удобен на этапе разработки:
    # - автоматическая перезагрузка при изменениях
    # - подробные сообщения об ошибках
    app.run(debug=True)