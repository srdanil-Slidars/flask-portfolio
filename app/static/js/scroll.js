/**
 * Файл для логики прокрутки.
 *
 * Позже сюда добавим:
 * - плавный скролл
 * - scroll-trigger эффекты
 * - подсветку активного раздела
 */

document.addEventListener("DOMContentLoaded", () => {
    // Заглушка под будущую работу со скроллом
});